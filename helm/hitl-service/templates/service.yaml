apiVersion: v1
kind: Service
metadata:
  name: hitl-service
  labels:
    app: hitl-service
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: hitl-service
